# No-Full-Diagonal Matching Matrices

This directory documents the SuiteSparse matrices exposed in the build tree at
`tests/data/no_full_diag/` for `maximum_matching_test.cpp`.

The matrices are square, structurally full rank, and missing at least one
explicit diagonal entry, so a correct maximum matching cannot rely on the
identity permutation.
