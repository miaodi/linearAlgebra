# Scratch Test Folder

This is a pure scratch folder for random tests and experiments.

## Usage

Edit `test.cpp` with your test code and rebuild:

```bash
cd release  # or debug
make scratch_test
./scratch/scratch/scratch_test
```

## Note

Files in this directory are ignored by git (except CMakeLists.txt, test.cpp, and this README).
